agent的输出，目前agent loop的最终输出就是agent对外界的完整响应，这其实没问题。但职责或存在不清晰的地方：
1. 目前的tool和current task prompt是按需披露的，相当于在不同的阶段告诉llm你能干什么，这个设计是非常好的，相当于时刻渲染llm能看到的前端。但是我们的代码，这些用来给llm的上下文里渲染“前端UI”的部分却分散在各处，我们难以维护。
2. 目前tool的调用还是比较鸡肋的，我们核心之需要agent做个选择或说句话，tool只是把agent的格式化输出又格式成了server能看懂的json+llm能看懂的“你做了什么”，其实还是前端UI的交互思路（类比于点击按钮的实时前端反馈+给系统实际发送的消息）。我们目前都是把这些逻辑零散地散落在各个地方，没有形成一整套核心组件来操控他们。

所以我觉得，是不是有个新概念——面向llm的UI，用以在llm上下文里渲染其应该了解的信息和可以执行的操作？